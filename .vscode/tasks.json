{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cargo: build",
      "detail": "Только компиляция zed.exe. Чтобы запустить сервер — F5 (Run and Debug).",
      "type": "process",
      "command": "cargo",
      "args": ["build", "--bin", "zed"],
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {
          "RUST_BACKTRACE": "1"
        }
      },
      "problemMatcher": ["$rustc"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "cargo: test",
      "type": "process",
      "command": "cargo",
      "args": ["test"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$rustc"],
      "group": "test"
    }
  ]
}
